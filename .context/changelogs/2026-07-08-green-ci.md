# 2026-07-08 — E01 green-ci: CI green on a credential-less clean checkout

> **Status**: Historical · **Created**: 2026-07-08 · **Source**: epic [20260708-green-ci](../epics/20260708-green-ci/); branch `fix/remediation-2026-07-07` commits `d6aa79f..d3883e7`

Epic E01 made the full pipeline pass on a fresh clone with **no AWS credentials** on Node 22: `install → stub → lint → test → typecheck → saas build → landing generate`, all green. Verified twice in a scratch clone with `AWS_*` env unset. The typecheck baseline went from **341 errors to 0**.

## What landed

| Area | Change |
|---|---|
| Credential-less config | Committed `layers/amplify/amplify_outputs.stub.json` (schema-valid, unmistakably fake) + a root `amplify:outputs:stub` copy script; CI copies it into the gitignored real path before install (design: `.context/epics/20260708-green-ci/design.md`, option a). The real `amplify_outputs.json` stays gitignored. |
| Module resolution (Phase 1) | Killed all 52 `TS2307`: declared the backend schema + `@aws-amplify/api-graphql` type deps in the amplify layer; appended `.ts`/`.vue` to the `exports` maps of the saas/auth/workspaces/entitlements layers; declared `aws-amplify`/`@nuxt/ui`/`zod` peers on the layers that import them; fixed a wrong-depth relative import and a non-exported backend deep import. Resolving `backend/schema` alone dissolved most of the cascade (341→146). |
| Typecheck burn-down (Phase 2) | ~95 behaviour-preserving type fixes across billing, entitlements, auth, debug, workspaces, the saas shell and `apps/saas/app`: implicit-any annotations, undefined guards, `error instanceof Error` narrowing, `import type`, and narrow commented casts at genuinely open-ended Amplify/Stripe/@nuxt-ui boundaries. |
| Toolchain | Node pinned to 22 (root + app `engines`, `.nvmrc`, `.npmrc engine-strict=true`) — `pnpm lint` crashed on Node 20 via `Object.groupBy`. |
| CI workflow | `ci.yml` provisions the stub before install; no AWS actions or repo secrets. |
| BUG-10 (clean-checkout build) | The five layer `tsconfig.json` files referenced `./.nuxt/tsconfig.*.json` that only a per-layer `nuxt prepare` generates (layers have no `postinstall`), breaking `nuxt build`/`generate` on any clean checkout. Dropped the dead project references (behaviour-neutral — no `compilerOptions`, the app tsconfig covers those files). |

## Debt resolved

`BUG-09` (CI red-by-design) and `BUG-14` (lint crash on Node 20) deleted from the ledger. `BUG-10` narrowed to its E09 remainder (the `apps/landing/amplify.yml` SSR-vs-SSG contradiction).

## Debt surfaced (deferred to E02)

Resolving module resolution exposed pre-existing latent defects, each preserved verbatim behind a narrow suppression + `TODO(E02)`:
- **BUG-15** — `useEntitlements` destructures `user` from `useUser()` (which exposes `currentUser`); `user` is `undefined` at runtime.
- **BUG-16** — model-scope `allow.resource()` in `apps/backend/amplify/data/resource.ts` no longer type-checks under `@aws-amplify/data-schema@1.26` (`BaseAllowModifier` omits `.resource`); `ampx` deploys it fine (backend has no strict typecheck). Durable fix: move the grants to schema scope, as the schema already does for `postConfirmation`.
- **BUG-17** — `getPaymentMethodInfo` reads `invoice.payment_intent`, removed from `Stripe.Invoice` in stripe-node v18; invoices silently lose card brand/last4/exp.

Several dev-only `/debug` page defects (dead `appConfig.billing.plans`, missing `useStripe`, wrong `createCheckoutSession` args) are likewise flagged `TODO(E02)`.

## Not done here

AC1/AC2 (`ci.yml` present and a green run **on master**) require the PR to merge — the first real run fires on the `pull_request` trigger. The auth-critical amplify-layer suppressions (server `generateClient` authMode) are behaviour-preserving stopgaps to revisit in E02/E10.
